# https://serg-cs.github.io/silo/docs/

# https://serg-cs.github.io/silo/docs/configuration/#image-options
# image.dockerfile = "containers/Dockerfile"
# container.cpus = 4
# container.memory = "6G"
# container.sudo = true
# shell = "zsh"
workspace.read_only = [".git"]

# https://serg-cs.github.io/silo/docs/configuration/#host-ports
# host_ports = [5432, 8080]

# https://serg-cs.github.io/silo/docs/configuration/#binds
[binds]
# docs = { source = "~/Documents/docs", target = "~/docs", access = "read-only" }
# output = { source = "./output", target = "./output", access = "read-write" }

# https://serg-cs.github.io/silo/docs/configuration/#managed-state
[state.project]
# cargo = { target = "~/.cargo" }
# cargo-target = { target = "./target" }

[state.user]
# codex = { target = "~/.codex" }

# https://serg-cs.github.io/silo/docs/configuration/#quick
[quick]
# test = ["cargo", "test"]
# agent = ["codex"]
