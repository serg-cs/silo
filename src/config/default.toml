# https://serg-cs.github.io/silo/docs/

# https://serg-cs.github.io/silo/docs/configuration/#image-options
# image.dockerfile = "/absolute/path/to/Dockerfile"
# container.cpus = 4
# container.memory = "6G"
# shell = "zsh"
read_only_git = true

# https://serg-cs.github.io/silo/docs/configuration/#forwards
[forward]
# postgres = { port = 5432, enabled = true }
# redis = { port = 6379, enabled = false }

# https://serg-cs.github.io/silo/docs/configuration/#host-mounts
[mounts.host]
# docs = { source = "~/Documents/docs", target = "~/docs" }
# output = { source = "./output", target = "./output", writable = true }

# https://serg-cs.github.io/silo/docs/configuration/#managed-mounts
[mounts.project]
# cargo = { target = "~/.cargo" }
# cargo-target = { target = "./target" }

[mounts.shared]
# codex = { target = "~/.codex" }

# https://serg-cs.github.io/silo/docs/configuration/#quick
[quick]
# test = ["cargo", "test"]
# agent = ["codex"]
